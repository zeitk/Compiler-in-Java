record Point (
    integer x;
    integer y;
);
boolean e(integer x, boolean b) {
    integer a;
    boolean c;
    integer y;
    return(c);
}
void a(integer b, boolean c) {
	integer x;
	record Point p;
        print <- p.x;
        print <- e(p.x, c);
        print <- x;
        print <- c;
        print <- 1 + 2;
	print <- true;
        print <- "String";
	print <- e(x,c);
	a(b, c);
	return;
}
integer y;
integer f(integer x, boolean b) {
    integer p;
    boolean c;
    boolean z;
    record Point pi;
    a(p, b);
    e(x, b); 
    scan -> pi.x;
    scan -> pi.y;
    scan -> x;
    scan -> p;
    scan -> b;
    scan -> f(p, b);
    return(1 + p);
}
void g() {
    integer a;
    boolean b;
    integer y;
    f(a, b);
    g();
    return;
}
integer x() {
    integer a;
	integer b;
	integer c;
	boolean y;
	a = b = c;
	f(a*b*c+3+3*b, y);
	return 1;
}
